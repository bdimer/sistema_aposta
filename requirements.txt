# FastAPI fornece a estrutura da API HTTP e gera a documentação Swagger.
fastapi==0.116.1

# SQLAlchemy é o ORM responsável por mapear classes Python para tabelas SQL.
SQLAlchemy==2.0.43

# Pydantic valida configurações, entradas e saídas da API.
pydantic==2.12.5

# Uvicorn é o servidor local que executa a aplicação FastAPI.
uvicorn[standard]==0.35.0

# python-dotenv carrega variáveis privadas definidas no arquivo .env.
python-dotenv==1.2.2

# Requests continua sendo usado pela integração existente com a API de futebol.
requests==2.34.2

# EmailStr, do Pydantic, utiliza este pacote para validar endereços de e-mail.
email-validator>=2.2,<3

# Pwdlib aplica hash seguro às senhas; Argon2 é o algoritmo utilizado.
pwdlib[argon2]>=0.2,<1

# PyJWT cria e valida tokens usados para manter o usuário autenticado.
PyJWT>=2.10,<3

# Permite que o FastAPI receba formulários usados pelo login OAuth2.
python-multipart>=0.0.20,<1

# Pytest executa e organiza os testes automatizados.
pytest>=8.4,<9

# HTTPX permite testar endpoints FastAPI sem abrir um servidor real.
httpx>=0.28,<1